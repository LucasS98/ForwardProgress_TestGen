module prod_cons_lobe(common) is

process MAIN [EX: ExChan, TERMINATE: NatChan] is
  par EX, TERMINATE in
    SHARED_MEMORY [EX, TERMINATE] (2)
  ||
    SCHEDULER_LOBE [EX, TERMINATE] ($N_THREADS$, 1)
  ||
    par

$REPLACE$

    end par
  end par
end process

end module
